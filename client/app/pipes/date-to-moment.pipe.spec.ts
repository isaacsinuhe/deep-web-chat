import { DateToMomentPipe } from './date-to-moment.pipe';

describe('DateToMomentPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToMomentPipe();
    expect(pipe).toBeTruthy();
  });
});
