import { MapAsArrayPipe } from './map-as-array.pipe';

describe('MapAsArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MapAsArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
